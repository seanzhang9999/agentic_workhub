import test from "node:test";import assert from "node:assert/strict";import { mkdtempSync } from "node:fs";import { tmpdir } from "node:os";import path from "node:path";import { ReceiptStore } from "../src/receipts.js";
test("replays only identical successful operations",()=>{const store=new ReceiptStore(path.join(mkdtempSync(path.join(tmpdir(),"awiki-receipts-")),"db.sqlite"));assert.equal(store.begin("op-12345678","append",{a:1}).replay,false);store.succeed("op-12345678",{ok:true});assert.equal(store.begin("op-12345678","append",{a:1}).replay,true);assert.throws(()=>store.begin("op-12345678","append",{a:2}),/different input/);});
